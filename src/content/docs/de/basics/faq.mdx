---
title: "Häufig gestellte Fragen"
description: "Häufige Fragen und Antworten zu Procaptcha."
i18nReady: true
---

### Was bedeutet `Site Key not Registered`?
Sie können auf diesen Fehler stoßen, wenn Sie das Procaptcha Widget auf Ihrer Website einrichten. Dieser Fehler tritt auf, wenn der sitekey,
den Sie verwenden, nicht im Prosopo-System registriert ist. Um diesen Fehler zu beheben, klicken Sie auf den Aktivierungslink in der E-Mail
oder <a href="mailto:support@prosopo.io">kontaktieren Sie das Prosopo-Supportteam</a>.

### Was bedeutet `Invalid Site Key`?
Dieser Fehler tritt auf, wenn der sitekey, den Sie verwenden, nicht gültig ist. Um diesen Fehler zu beheben, stellen Sie sicher, dass Sie den
richtigen sitekey verwenden, den Sie finden können, indem Sie sich beim [Prosopo Portal](https://portal.prosopo.io) anmelden.

### Was bedeutet `Unauthorized origin URL`?
Dieser Fehler tritt auf, wenn die Domain des sitekey nicht mit der Domain der Website übereinstimmt, auf der das Procaptcha Widget
verwendet wird. Um diesen Fehler zu beheben, stellen Sie sicher, dass die Domain des sitekey mit der Domain der Website übereinstimmt, indem Sie
sich beim [Prosopo Portal](https://portal.prosopo.io) anmelden und die Domain aktualisieren.

### Was bedeutet `Incorrect CAPTCHA type`?
Dieser Fehler tritt auf, wenn der [CAPTCHA-Typ](/de/basics/captcha-types) nicht korrekt gesetzt ist. Der Standard-`captchaType` ist
`frictionless`. Wenn Sie `pow` oder `image` verwenden möchten, müssen Sie Ihren `captchaType` in der Widget-Konfiguration auf `pow`
oder `image` setzen. Sie müssen auch Ihre [Portaleinstellungen](https://portal.prosopo.io) entsprechend aktualisieren.

### Was bedeutet `Session not found`?
Dieser Fehler tritt auf, wenn die Sitzung nicht gefunden wird. Dies kann passieren, wenn die Sitzung abgelaufen ist oder wenn die Sitzungs-ID
nicht gültig ist. Um diesen Fehler zu beheben, warten Sie entweder, bis das Widget neu geladen wird (3s), oder aktualisieren Sie die Seite. Wenn der Fehler weiterhin besteht,
versuchen Sie, Ihren Browser-Cache und Cookies zu löschen. Wenn der Fehler weiterhin besteht, [kontaktieren Sie bitte das Prosopo-Supportteam](https://discord.com/invite/3nMYAHecZT).

### Was bedeutet `Error parsing captcha`?
Dieser Fehler tritt auf, wenn falsche Daten mit der CAPTCHA-Antwort gesendet wurden. Um diesen Fehler zu beheben, stellen Sie sicher, dass die
mit der CAPTCHA-Antwort gesendeten Daten korrekt sind.




